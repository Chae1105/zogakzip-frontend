/** @type {import('tailwindcss').Config} */
export default {
  content: ["./src/**/*.{js, jsx, ts, tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};

// src 폴더 아래 모든 하위 폴더에서 js, jsx, ts, tsx 파일에 tailwind css 적용
